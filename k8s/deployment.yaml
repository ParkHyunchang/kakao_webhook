apiVersion: v1
kind: Namespace
metadata:
  name: kakao-webhook

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kakao-webhook
  namespace: kakao-webhook
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kakao-webhook
  template:
    metadata:
      labels:
        app: kakao-webhook
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: kakao-webhook
          image: ghcr.io/parkhyunchang/kakao_webhook:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3500
          env:
            - name: VLLM_BASE_URL
              value: "http://qwen2-5-vl-32b-awq.vllm.svc.cluster.local:8000"
            - name: VLLM_MODEL
              value: "qwen2.5-vl-32b-instruct-awq"
            - name: VLLM_TIMEOUT
              value: "55"
            - name: LOG_DIR
              value: "/app/logs"
            - name: TZ
              value: "Asia/Seoul"
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: kakao-webhook
  namespace: kakao-webhook
spec:
  type: NodePort
  selector:
    app: kakao-webhook
  ports:
    - port: 3500
      targetPort: 3500
      nodePort: 30350
